# Стратегическое виденье и позиционирование

**Neverwhere** — это узкоспециализированный 2D engine/suite: **Epic Map Editor** (редактор) + **EpicGameClient** (игровой клиент). Редактор создаёт и редактирует игровые данные, клиент запускает “тот же мир” для проверки и плейтеста — через общий рендер (`render_core/WorldRenderer`), по паритету с отрисовкой редактора.

## 🎯 Позиционирование
**Epic Map Editor** задуман как специализированная платформа для создания системных 2D-игр (System-driven games). Мы занимаем нишу "Золотой середины":
*   Гибче и мощнее, чем **RPG Maker** (не ограничены жанром JRPG и тайловой сеткой одного слоя).
*   Удобнее и быстрее, чем **Unity/Godot** (предоставляем готовые высокоуровневые инструменты для жанра "из коробки", а не заставляем писать инвентарь с нуля).

**Целевые жанры:**
*   Farming Sims / Симуляторы жизни (Stardew Valley, Harvest Moon).
*   Action-RPG / Hack-and-Slash (Diablo, Torchlight).
*   Менеджмент и экономические стратегии (Graveyard Keeper).
*   Рогалики (Roguelikes).

---

## 🚀 Ключевые возможности (Vision Pillars)

> **Легенда статусов:** `[в работе]` — что-то уже есть в коде; `[заготовка]` — есть
> только data-модель/каркас без инструмента; `[концепт]` — идея, кода нет.
> Статусы сверены с репозиторием на дату обновления документа.

### 1. Бесшовный рабочий процесс (Seamless Workflow) — `[в работе]`
> **Что есть:** `game_runtime::Fixture` (билдер фикстур, см. `src/libs/game_runtime`),
> общий рендер карты в редакторе и клиенте (`MapRenderItem` на `WorldRenderer`),
> и **Play-Test внутри редактора**: кнопка ▶ на канвасе карты открывает игровую
> вкладку (`TabType.Game`) с изолированным `game_runtime::Runtime` + сессией из
> фикстуры (`newGame()`-мок). Одна вкладка на главу (дедуп по имени «▶ <глава>»),
> повторный Play перезапускает сессию. Камера стартует с позиции редактора,
> тик — только когда вкладка видима. RPC-автоматизация редактора
> (127.0.0.1:9877): `play`, `load_chapter`/`create_chapter`, авторинг в
> координатах клеток (`set_tile`/`fill_rect`/`set_landscape`), чтение —
> `get_map`, визуальная проверка — `screenshot`; MCP-обёртка `neverwhere-editor`
> (см. AGENTS.md).
*   **Мгновенный Play-Test:** `[есть]` — запуск главы прямо из редактора игровой вкладкой (см. выше). Отдельный запуск через **EpicGameClient** также остаётся.
*   **Горячая перезагрузка:** Возможность менять параметры баланса на лету, не перезапуская runtime.
*   **Fixtures для игровых данных:** если runtime-логике нужны данные, которых нет в текущей карте (профиль игрока, прогресс, инвентарь), редактор подсовывает их как **фикстуры** (по аналогии с unit tests): “профиль по умолчанию”, “тестовый инвентарь”, временное in-memory хранилище на время сессии плейтеста. Управление моками профиля — следующий шаг.

### 2. Визуальное превосходство ландшафта (Advanced Landscape) — `[в работе]`
> **Что есть:** `src/libs/landscape_core`, `landscape_mesh`, `render_core`
> (`WorldRenderer` — фасад единого рендера мира: `LandscapeRenderer` + `SpriteRenderer`
> + `OverlayRenderer`), клиент `EpicGameClient` на этом ядре (паритет с редактором),
> анализ тайлов в `docs/TILE_RESOLUTION_ANALYSIS.md`, плюс песочницы
> (`PolygonalGeneratedLandscapePlayground`, `SplattingPlayground`).
> **Поднятая 3D-земля `[есть]`:** порт TileShapePlayground — ассеты `shape3d`
> (атлас + `raisedHeight`/RockWalls-параметры) рисуются `Shape3dPencil` на слое
> `RaisedLandscape` (vertex-centric, своя сетка нод); рендер — стены + верх со
> сдвигом в `LandscapeRenderer`, паритет с клиентом через `WorldFrame.raisedTiles`.
> **Cliff 3D `[есть]`:** ассеты `cliff3d` (полный набор параметров клиф-генератора
> + палитра) кистью `CliffPencil` на слое `CliffLandscape`; рендер —
> `CliffRenderer` (scalar field → surface nets, кэш с debounce) с паритетом
> редактор/клиент; панель параметров в правой колонке.
*   **Многослойность:** Поддержка множества слоев высоты и тайлов.
*   **Smart Blending:** Продвинутая система смешивания материалов (авто-переходы песок -> трава -> камень) без видимых швов.
*   **Авто-декорирование:** Процедурная кисть для нанесения декалей (камни, цветы, трещины) и вариаций тайлов, чтобы избежать "тайлового шума".

### 3. Глубокая процедурная генерация (Deep Generation) — `[концепт]`
*   **Генерация Смысла, а не только шума:** Генератор создает не просто карту высот, а логически связные структуры (деревня с жителями, подземелье с боссом).
*   **Вплетение сюжета:** Автоматическое размещение квестовых NPC и предметов в сгенерированном мире.
*   **Indoor/Outdoor:** Поддержка бесшовной генерации интерьеров зданий внутри глобальной карты мира.

### 4. "Живой Мир": Атмосфера и Время — `[концепт]`
*   **Timeline-режиссер:** Инструмент для настройки суточного цикла и погоды. "В 19:00 включить фонари, изменить тон освещения, запустить звук сверчков".
*   **Динамическое освещение 2D:** Зоны света, тени от объектов, реагирующие на время суток.

### 5. Поведение и Расписание (Behavior & Schedules) — `[концепт]`
*   **Редактор жизни NPC:** Визуальная шкала расписания для симуляторов жизни (Утро: Дом -> Работа; Вечер: Таверна).
*   **AI Patterns:** Конструктор поведения врагов для Action-RPG (Видит игрока -> Атака; Мало HP -> Побег) без написания кода.

### 6. База Данных Сущностей (Entity Database) — `[концепт]`
*   **Табличный редактор баланса:** Встроенный аналог Excel для управления тысячами предметов.
*   **Наследование:** Создание архетипов ("Базовый Меч") и вариаций ("Огненный Меч +1").
*   **Визуальный Лут:** Настройка таблиц выпадения предметов (Drop Tables) с визуализацией вероятностей.

### 7. Интерактивность "Из коробки" — `[концепт]`
*   **Destructible & Harvestable:** Система разрушаемого окружения (рубка деревьев, разбивание бочек) на уровне галочки в свойствах объекта. Не нужно программировать получение урона для каждого куста.

### 8. Нарративный Дизайн (Quests & Dialogs) — `[концепт]`
*   **Нодовый редактор диалогов:** Графы диалогов с условиями (проверка инвентаря, отношений).
*   **Smart Flow:** Визуализация переходов между локациями (телепорты, двери) в виде связей, где видно превью целевой локации.

---

## Ближайшие технические задачи
- **Undo/Redo**: перейти на неизменяемые снэпшоты через `immer` (подробнее — `docs/TECHNICAL_STACK.md`).
- **Data/View separation**: отделить данные от `QObject`/QML-представлений (частью сделано — есть чистый слой `game_data`).

---

## Референсы (Inspiration)
*   **Stardew Valley:** Уют, циклы, крафт, социалка.
*   **Graveyard Keeper:** Сложные цепочки крафта, экономика, технологии.
*   **Diablo:** Боевка, лут, генерация подземелий, атмосфера.
