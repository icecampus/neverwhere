# PGG — язык процедурной генерации геометрии

Вся документация по PGG собрана здесь.

| Документ | Содержание |
|---|---|
| `geometry_generation_language.md` | **Спецификация языка** — единственный источник правды по семантике: типы и атрибуты (§4), воспроизводимость и зоны (§5), синтаксис (§6), композиция (§7), builtin'ы (§8), отладка (§9), нодовая проекция (§10), диагностики (§11), этапы и критерии (§15), история изменений (§19) |
| `implementation.md` | **Заметки по реализации** — этапы E0–E8 по файлам (`src/libs/pgg`), инженерные грабли ANTLR/ядра, CLI PggTool и PggViewer, корпус примеров `src/tests/pgg/corpus/` и сьюты тестов. Правя код PGG, обновляй его |

Код: `src/libs/pgg` (библиотека), `src/apps/PggTool` (CLI), `src/apps/PggViewer` (нодовая проекция + превью), `src/tests/pgg` (тесты + корпус `.pgg`-примеров).
